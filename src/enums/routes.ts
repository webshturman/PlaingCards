export enum PATH {
  // LAYOUT = '/',
  PROFILE = '/profile',
  LOGIN_FORM = '/',
  LOGIN = 'login',
  REGISTER = 'register',
  NEW_PASSWORD = 'new_pass',
  RECOVERY_PASSWORD = 'recovery',
  ERROR = '404',
  WRONG_PAGE = '*',
  TEST_PAGE = 'test',
  CARDS = '/cards/',
  CARDS_TABLE = 'cards_table',
}
