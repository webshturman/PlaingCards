export enum PATH {
  PROFILE = '/',
  REGISTER = '/register',
  LOGIN = '/login',
  NEW_PASSWORD = '/new_pass',
  RECOVERY_PASSWORD = '/recovery',
  ERROR = '/404',
  WRONG_PAGE = '/*',
  TEST_PAGE = '/test',
}
